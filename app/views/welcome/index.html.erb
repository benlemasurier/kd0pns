<% @posts.each do |post| %>
  <article>
    <h3><%= post.title -%></h3>
    <h4 class="date"><%= time_ago_in_words(post.created_at) -%> ago</h4>
    <p>
      <%= post.content -%>
    </p>
  </article>
<% end %>
